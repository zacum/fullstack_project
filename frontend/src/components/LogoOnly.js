import React from 'react';
import { Box } from '@mui/material';

const LogoOnly = () => {
  return <Box component="img" sx={{ width: 100 }} src="/logos/logo_only.png" alt="gunthy log" />;
};

export default LogoOnly;
